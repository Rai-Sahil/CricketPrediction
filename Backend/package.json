{
  "name": "backend",
  "version": "1.0.0",
  "description": "This is for sports project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc",
    "dev": "ts-node-dev --respawn --transpile-only ./app/server.ts",
    "prod": "tsc && node ./build/server.js"
  },
  "author": "Sahil rai",
  "license": "ISC",
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@types/express": "^4.17.21",
    "express": "^4.18.2"
  }
}
